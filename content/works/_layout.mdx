---
type: work
---

import {
  Label,
  Summary,
  Metadata,
  RequiredStatement,
  References,
} from "@canopy-iiif/app/ui/server";

<Container variant="wide" className="canopy-work--layout">
  <div className="canopy-work--primary">
    <Viewer iiifContent={props.manifest.id} />
  </div>
  <div className="canopy-work--secondary">
    <header>
      <Label manifest={props.manifest} as="h1" />
      <Summary manifest={props.manifest} as="p" className="canopy-lead" />
    </header>
    <div>
      <References />
      <Metadata manifest={props.manifest} />
      <RequiredStatement manifest={props.manifest} />
      <Id id={props.manifest.id} />
    </div>
  </div>
</Container>
{/* <Container>
  <Scroll
    iiifContent={props.manifest.id}
    options={{
      offset: 16,
      figure: {
        aspectRatio: 1,
        display: "thumbnail",
        width: "200px",
      },
    }}
  />
</Container> */}
{(() => {
  const np = props.manifest?.navPlace || props.manifest?.navplace;
  const features = Array.isArray(np?.features) ? np.features
    : np?.type === 'Feature' ? [np]
    : [];
  if (!features.length) return null;
  const title = (props.manifest?.label?.en || props.manifest?.label?.none || [])[0] || '';
  return (
    <Container>
      <h2>Location</h2>
      <Map tileLayers={[
        {
          name: "Carto Positron (Light)",
          url: "https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png",
          attribution: "&copy; OpenStreetMap &copy; CARTO",
          maxZoom: 20
        },
        {
          name: "Carto Voyager",
          url: "https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png",
          attribution: "&copy; OpenStreetMap &copy; CARTO",
          maxZoom: 20
        },
        {
          name: "Carto Dark Matter",
          url: "https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png",
          attribution: "&copy; OpenStreetMap &copy; CARTO",
          maxZoom: 20
        },
        {
          name: "Esri Satellite",
          url: "https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}",
          attribution: "Tiles &copy; Esri",
          maxZoom: 19
        },
        {
          name: "OpenStreetMap",
          url: "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",
          attribution: "&copy; OpenStreetMap contributors",
          maxZoom: 19
        },
        {
          name: "Thunderforest Pioneer",
          url: "https://api.thunderforest.com/pioneer/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        },
        {
          name: "Thunderforest Landscape",
          url: "https://api.thunderforest.com/landscape/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        },
        {
          name: "Thunderforest Outdoors",
          url: "https://api.thunderforest.com/outdoors/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        },
        {
          name: "Thunderforest OpenCycleMap",
          url: "https://api.thunderforest.com/cycle/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        },
        {
          name: "Thunderforest Neighborhood",
          url: "https://api.thunderforest.com/neighbourhood/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        },
         {
          name: "Thunderforest Atlas",
          url: "https://api.thunderforest.com/atlas/{z}/{x}/{y}.png?apikey=030c864a552447eb8830a67a6a45bc9b",
          attribution: "&copy; Thunderforest &copy; OpenStreetMap contributors",
          maxZoom: 22
        }
    ]}>
        {features.map((feature, i) => {
          const coords = feature?.geometry?.coordinates || [];
          if (coords.length < 2) return null;
          return (
            <MapPoint
              key={i}
              lat={String(coords[1])}
              lng={String(coords[0])}
              title={title}
              referencedManifests={[props.manifest.id]}
            />
          );
        })}
      </Map>
    </Container>
  );
})()}
<Container>
  <h2>Related Items</h2>
  <RelatedItems iiifContent={props.manifest.id} top={3} />
</Container>
